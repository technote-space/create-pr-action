import { SemVer } from 'semver-utils';
import { UpgradeGroup } from './UpgradeGroup';
/** Supported function for the --group options. */
export declare type GroupFunction = (packageName: string, currentVersionSpec: SemVer[], upgradedVersionSpec: SemVer[], upgradedVersion: SemVer | null, defaultGroup: UpgradeGroup) => UpgradeGroup;
